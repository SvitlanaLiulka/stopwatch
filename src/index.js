import React from 'react';
import ReactDOM from 'react-dom';
import StopWatch from './componens/stopwatch/stopwatch';

ReactDOM.render(
  <React.StrictMode>
    <StopWatch />
  </React.StrictMode>,
  document.getElementById('root')
);


